extends ../auth

block content
  .main-content(role='main')
    .wrapper
      .auth-box
        h1 تاییدیه ایمیل
        - if (!user || user.email_verified)
          p.msg 
            | ایمیل شما تایید شده بوده
        - else
          p.msg لینک تایید برای شما ایمیل شد.
        br
        |  <a href="/">بازگشت به صفحه اول.</a>
